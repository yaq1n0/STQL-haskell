IMPORT foo foo.ttl 
IMPORT bar bar.ttl
NEW out out1.ttl
// same as NEW out1.ttl then IMPORT out1.ttl AS out

WHERE TRUE
ADDTO out foo.all
ADDTO out bar.all //ADD bar.all is equivalent to ADD bar.subj, bar.pred, bar.obj